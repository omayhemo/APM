# Claude Code Deployment Variables
# Used for Claude Code persona deployment

export PROJECT_ROOT="{{PROJECT_ROOT}}"
export CLAUDE_DIR="{{CLAUDE_DIR}}"
export CLAUDE_COMMANDS_DIR="{{CLAUDE_COMMANDS_DIR}}"
export PROJECT_DOCS="{{PROJECT_DOCS}}"
export BACKLOG_PATH="{{BACKLOG_PATH}}"
export SESSION_NOTES_PATH="{{SESSION_NOTES_PATH}}"
export RULES_PATH="{{RULES_PATH}}"
export ARCHIVE_PATH="{{ARCHIVE_PATH}}"

# Voice notification scripts (optional for Claude)
export SPEAK_ORCHESTRATOR="{{SPEAK_ORCHESTRATOR}}"
export SPEAK_DEVELOPER="{{SPEAK_DEVELOPER}}"
export SPEAK_ARCHITECT="{{SPEAK_ARCHITECT}}"
export SPEAK_ANALYST="{{SPEAK_ANALYST}}"
export SPEAK_QA="{{SPEAK_QA}}"
export SPEAK_PM="{{SPEAK_PM}}"
export SPEAK_PO="{{SPEAK_PO}}"
export SPEAK_SM="{{SPEAK_SM}}"
export SPEAK_DESIGN_ARCHITECT="{{SPEAK_DESIGN_ARCHITECT}}"

# Claude configuration files
export AP_ROOT="{{AP_ROOT}}"
export AP_CONFIG_FILE="{{AP_CONFIG_FILE}}"
export AP_KB_FILE="{{AP_KB_FILE}}"
export AP_CHECKLISTS="{{AP_CHECKLISTS}}"
export AP_DATA="{{AP_DATA}}"
export AP_PERSONAS="{{AP_PERSONAS}}"
export AP_TASKS="{{AP_TASKS}}"

# Template metadata
export MIGRATION_VERSION="{{MIGRATION_VERSION}}"
export AGENT_NAME="{{AGENT_NAME}}"

# Deployment target specific
export DEPLOYMENT_TARGET="claude"
export VOICE_INTEGRATION="optional"
export SESSION_MANAGEMENT="claude"
export WORKSPACE_ENFORCEMENT="guided"
export INITIALIZATION_MODE="claude_sequence"
export SUBAGENT_COORDINATION="native"